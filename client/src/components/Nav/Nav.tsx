import React, { Component } from 'react'
import "./Nav.scss"

export default class Nav extends Component {
    render() {
        return (
            <nav className = "Nav">
                
            </nav>
        )
    }
}
